#Optional - only if you are running LW CodeScan and venv
default:
  sca:
    enabled: true
    scan:
      sast: true
      exclude:
        - ".venv/**"
      exceptions:
        - "finding:9C7420D532199729:False positive"
