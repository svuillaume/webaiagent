services:
  webai:
    build: .
    container_name: webai-serve
    ports:
      - "8765:8765"
    env_file:
      - .env
    volumes:
      - ~/.lacework.toml:/root/.lacework.toml:ro
      - ~/.config/lacework:/root/.config/lacework
      - ~/claude_cnapp/lql/lql_queries:/lql_queries:ro
    restart: unless-stopped
